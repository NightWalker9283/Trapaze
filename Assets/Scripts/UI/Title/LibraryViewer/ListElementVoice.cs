using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//アーカイブ画面。取得ボイス一覧の要素オブジェクト雛形。パスの保持のみ
public class ListElementVoice : MonoBehaviour
{
    public string path;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
